# PaddleOCR 文档处理服务依赖

# FastAPI Web 框架
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# OCR 引擎（中文优化）
paddleocr>=2.7.0
paddlepaddle>=2.5.0  # CPU 版本，如有 GPU 可换 paddlepaddle-gpu

# PDF 处理
PyMuPDF>=1.23.0

# 类型定义
pydantic>=2.0.0