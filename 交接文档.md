# 个人工作站 (Personal Workspace) — 项目交接文档

**版本**: 1.4.0  
**最后更新**: 2026-04-29  
**技术栈**: React 18 + TypeScript + Webpack 5 + Tailwind CSS 3

---

## 目录

1. [项目概述](#1-项目概述)
2. [开发环境](#2-开发环境)
3. [项目结构](#3-项目结构)
4. [构建配置](#4-构建配置)
5. [数据层 (localStorage)](#5-数据层-localstorage)
6. [路由架构](#6-路由架构)
7. [布局组件](#7-布局组件)
8. [页面详解](#8-页面详解)
9. [样式体系](#9-样式体系)
10. [已知问题与技术债务](#10-已知问题与技术债务)
11. [常见开发任务](#11-常见开发任务)
12. [近期更新记录](#12-近期更新记录)

---

## 1. 项目概述

个人工作站是一个**中文界面的单页应用**（SPA），面向个人效率场景。所有数据存储于浏览器 `localStorage`，并自动同步到项目 `data/` 目录（通过 Webpack Dev Server Middleware），**无独立后端服务**。应用基于 HashRouter 实现 6 个功能页面，共享统一的侧边栏 + 顶栏布局。

### 核心功能模块

| 模块 | 说明 |
|---|---|
| **仪表盘** | 聚合任务、笔记、财务统计数据的首页 |
| **笔记** | Markdown 编辑器，支持文件夹/标签/双向链接/图谱视图 |
| **任务** | 待办事项管理，优先级和截止日期 |
| **财务** | 收支记录与统计（面向 LLM API 费用场景） |
| **工具** | 计算器、取色器、JSON 格式化工具 |
| **设置** | 个人资料、通知、安全、数据导入导出、主题切换 |

---

## 2. 开发环境

### 常用命令

```bash
npm run dev        # 启动开发服务器 → http://localhost:3266（热更新）
npm run build      # 生产构建到 dist/bundle.js
npm run typecheck  # TypeScript 类型检查（tsc --noEmit）
```

### 依赖概览

**生产依赖**:
| 包名 | 用途 |
|---|---|
| react 18, react-dom 18 | 核心框架 |
| react-router-dom 6 | HashRouter 路由 |
| framer-motion | 动画（布局动画、入场过渡） |
| lucide-react | 图标库 |
| tailwindcss 3 | 原子化 CSS |
| @uiw/react-codemirror | CodeMirror 6 的 React 封装 |
| @codemirror/lang-markdown | Markdown 语法支持 |
| @replit/codemirror-vim | Vim 键位绑定 |
| react-markdown + remark-gfm | Markdown 渲染与 GFM 扩展 |
| d3 7 | 力导向图谱 |
| recharts | 图表（财务模块） |
| date-fns | 日期工具 |
| fuse.js | 模糊搜索（命令面板） |

**开发依赖**: Webpack 5 + CLI + dev-server, Babel (preset-env/react/typescript), TypeScript 5.3, PostCSS + autoprefixer, html-webpack-plugin, @types/react + @types/d3

### 第三方服务

无。所有内容离线运行，无外部 API 调用。

---

## 3. 项目结构

```
Table/
├── index.html                  # HTML 模板（Webpack 入口）
├── package.json
├── tsconfig.json               # TypeScript strict 模式
├── webpack.config.js           # Webpack 5 配置（含 sync middleware）
├── tailwind.config.js          # Tailwind + dark mode(class)
├── postcss.config.js
├── CLAUDE.md                   # Claude Code 项目指南
├── .claude/settings.local.json # Claude Code 权限配置
│
├── data/                       # 自动同步的数据目录
│   ├── README.md               # 数据目录说明
│   ├── notes/                  # 笔记按文件夹分组导出为 .md
│   ├── tasks/tasks.json        # 任务数据
│   ├── finance/finance.json    # 财务数据
│   ├── folders/folders.json    # 文件夹结构
│   └── backup/                 # 导出备份存档
├── scripts/
│   └── export-to-files.js      # CLI 脚本：JSON 备份 → 文件夹结构
├── dist/                       # 构建输出
├── node_modules/
│
└── src/
    ├── index.tsx                # React 入口（createRoot）
    ├── App.tsx                  # 路由配置（HashRouter）
    ├── styles/
    │   └── index.css            # Tailwind 指令 + CSS 变量 + 滚动条
    ├── contexts/
    │   └── ThemeContext.tsx     # 主题上下文（浅色/深色切换）
    ├── db/
    │   └── index.ts             # 数据层（4 集合 CRUD + auto-sync + dataManager）
    ├── components/
    │   ├── Layout/
    │   │   ├── index.tsx         # 布局外壳（侧边栏 + 顶栏 + Outlet）
    │   │   ├── Sidebar.tsx       # 导航侧边栏（深色背景）
    │   │   └── Header.tsx        # 顶栏（毛玻璃效果）
    │   ├── ErrorBoundary.tsx    # React 错误边界
    │   ├── Loading.tsx          # 加载状态组件
    │   ├── PinLock.tsx          # PIN 锁屏组件
    │   └── VirtualList.tsx      # 虚拟列表组件
    └── pages/
        ├── Dashboard/index.tsx   # 仪表盘聚合页
        ├── Notes/
        │   ├── index.tsx         # 笔记主页（三栏布局）
        │   ├── hooks/
        │   │   ├── useNotesLayout.ts   # 布局状态管理
        │   │   └── useNotesSearch.ts   # 搜索防抖与取消
        │   └── components/
        │       ├── Editor.tsx         # CodeMirror Markdown 编辑器
        │       ├── FileTree.tsx       # 文件树（递归文件夹）
        │       ├── Backlinks.tsx      # 双向链接面板
        │       ├── TagCloud.tsx       # 标签云
        │       ├── GraphView.tsx      # D3 力导向图谱
        │       └── CommandPalette.tsx # ⌘K 命令面板
        ├── Tasks/index.tsx       # 任务管理
        ├── Finance/index.tsx     # 收支统计
        ├── Tools/index.tsx       # 工具集（计算器/取色器/JSON格式化）
        └── Settings/index.tsx    # 设置页
```

---

## 4. 构建配置

### Webpack (webpack.config.js)

| 配置项 | 值 |
|---|---|
| Entry | `./src/index.tsx` |
| Output | `dist/bundle.js` |
| 默认端口 | 3266 |
| HTML 模板 | `./index.html` |
| Loader | babel-loader (.ts/.tsx/.js/.jsx) |
| CSS 链 | style-loader → css-loader → postcss-loader |
| historyApiFallback | 已启用，带 `_p/\d+/` 重写规则 |

### Dev Server Middleware (auto-sync)

Webpack Dev Server 通过 `setupMiddlewares` 注册了 `POST /api/sync-data` 端点：

- **触发时机**：数据层每次写操作（add/update/delete）后，经 1.5s 防抖，自动 POST 全量数据到 `/api/sync-data`
- **写入逻辑**（`writeSyncData()` 函数）：
  - 笔记按文件夹分组，每篇笔记写入 `data/notes/<文件夹名>/<标题>.md`，含 YAML front matter（id, tags, links, createdAt, updatedAt）
  - 任务数据写入 `data/tasks/tasks.json`
  - 财务数据写入 `data/finance/finance.json`
  - 文件夹结构写入 `data/folders/folders.json`
- **生产构建**：auto-sync 仅在 `npm run dev` 开发服务器中生效；生产构建 (`dist/bundle.js`) 不包含此功能

### TypeScript (tsconfig.json)

- **strict**: true
- **jsx**: react-jsx（自动运行时，无需显式 `import React`）
- **target**: ES2020
- **module**: ESNext
- **noEmit**: true（Babel 处理转译）
- **skipLibCheck**: true

### Tailwind

- 内容扫描路径：`./src/**/*.{js,jsx,ts,tsx}` + `./index.html`
- 自定义颜色：`primary: '#3b82f6'`（即 blue-500）
- 暗色模式策略：`class`（在 `<html>` 上切换 `.dark` 类）

---

## 5. 数据层 (localStorage)

**文件**: `src/db/index.ts`

### 数据结构

```typescript
// 财务记录
interface FinanceRecord {
  id: string;           // Date.now().toString()
  type: 'income' | 'expense';
  amount: number;
  description: string;
  category: string;
  date: string;         // ISO 日期
  model?: string;       // 仅支出有模型归属
}

// 任务
interface Task {
  id: string;
  title: string;
  completed: boolean;
  createdAt: string;    // ISO 日期
  priority: 'low' | 'medium' | 'high';
  dueDate?: string;
}

// 文件夹
interface Folder {
  id: string;
  name: string;
  parentId: string | null;
  createdAt: string;
  updatedAt: string;
}

// 笔记
interface Note {
  id: string;
  title: string;
  content: string;        // Markdown
  folderId: string | null;
  createdAt: string;
  updatedAt: string;
  tags: string[];
  links: string[];        // 出链笔记 ID 列表
  backlinks: string[];    // 入链笔记 ID 列表
}
```

### 存储键与集合

| localStorage Key | 集合类型 | 操作对象 |
|---|---|---|
| `finance_records` | `FinanceRecord[]` | `financeDB` |
| `tasks_records` | `Task[]` | `taskDB` |
| `notes_records` | `Note[]` | `noteDB` |
| `folders_records` | `Folder[]` | `folderDB` |

### 导出的 API

**`initDB()`** — 从 localStorage 重新加载所有集合。

**`financeDB`**
- `getAll()` → 按日期降序返回
- `add(record)` / `update(id, partial)` / `delete(id)`
- `getStats()` → `{ income, expense, profit }`
- `getModelStats()` → 按 model 分组的收支汇总

**`taskDB`**
- `getAll()` / `add()` / `update()` / `delete()`
- `toggle(id)` — 翻转 `completed` 状态
- `getStats()` → `{ total, completed, pending }`

**`noteDB`**
- `getAll()` / `getByFolder(folderId)` / `add()` / `update()` / `delete()`
- `search(query)` — 对 title 和 content 做大小写不敏感匹配
- `updateLinks(noteId, links[])` — 维护双向链接：新链接添加反向引用，移除的链接清理反向引用
- `getBacklinks(noteTitle)` → 返回所有指向该标题的笔记

**`folderDB`**
- `getAll()` / `getByParent(parentId)` / `add()` / `update()` / `delete()`
- `getTree()` → 根文件夹 + `children` 子文件夹（仅一层嵌套）
- 删除文件夹时，其下笔记的 `folderId` 会被置为 null；递归删除子文件夹

**`dataManager`**
- `exportAll()` → JSON 字符串（包含所有 4 个集合 + `version: 1` + 导出时间戳）
- `importAll(jsonString)` → 解析并校验（`Array.isArray()`），替换集合
- `clearAll()` → 清空所有集合
- `getStats()` → `{ finance, tasks, notes, folders, totalSize }`

### 重要架构说明

1. **不使用任何外部数据库** — 所有数据存储于 localStorage
2. **数据封装** — 内部数据数组已封装，外部通过 API 访问，不暴露内部引用
3. **自动同步（auto-sync）** — 每次写操作后触发 1.5s 防抖，通过 `fetch()` POST 到 `http://localhost:3266/api/sync-data`

---

## 6. 路由架构

**文件**: `src/App.tsx`

使用 `HashRouter`，所有路由是 `Layout` 的子路由：

| 路径 | 组件 | 说明 |
|---|---|---|
| `/` | — | 重定向到 `/dashboard` |
| `/dashboard` | Dashboard | 首页仪表盘 |
| `/notes` | Notes | 笔记（三栏布局） |
| `/tasks` | Tasks | 任务管理 |
| `/tools` | Tools | 工具集 |
| `/finance` | Finance | 财务统计 |
| `/settings` | Settings | 设置页面 |

---

## 7. 布局组件

### Layout (`src/components/Layout/index.tsx`)

外壳组件，flex 布局：
- 左侧：固定 `<Sidebar />`（`w-64`）
- 右侧：flex 列，包含 `<Header />` + `<main><Outlet /></main>`，`ml-64` 偏移

### Sidebar (`src/components/Layout/Sidebar.tsx`)

- 固定定位，深色背景 `#18181B`
- 6 个导航项，使用 Framer Motion `layoutId="activeTab"` 实现选中标签的平滑滑动动画
- 悬停时半透明背景叠加效果
- 底部显示用户头像占位（"我"）+ "在线" 状态

### Header (`src/components/Layout/Header.tsx`)

- 粘性定位，毛玻璃效果（`bg-white/80 backdrop-blur-xl`）
- 标题："个人工作站"
- 右侧按钮：铃铛、设置、用户头像

---

## 8. 页面详解

### 8.1 仪表盘 (Dashboard)

**文件**: `src/pages/Dashboard/index.tsx`

**数据源**: `financeDB`, `taskDB`, `noteDB`

**布局**:
- 标题区域（带日期）
- 4 个快速操作卡片（grid 4 列）：待办任务数、笔记数、净收益、今日日期
- 待办任务面板（span 2 列）：最多 3 个
- 最近笔记面板（1 列）：最多 3 个
- 费用概览面板（全宽）：收入/支出/净收益三栏

**深色模式**: 已适配，使用 `dark:` 前缀

### 8.2 笔记 (Notes) — 核心复杂度模块

**文件**: `src/pages/Notes/index.tsx` + 6 个子组件

**主页面 (`index.tsx`)**:
- 三栏布局（可拖拽调整宽度，可折叠）
- 左侧边栏：`FileTree` 组件
- 中间：编辑器或空状态
- 右侧边栏：`Backlinks` / `TagCloud` / `GraphView` 三 Tab 切换
- 拖拽分隔条通过 `mousedown/mousemove/mouseup` 实现
- `Ctrl/Cmd+K` 快捷键打开 `CommandPalette`
- 链接解析：支持 `[[笔记ID]]` 和 `[[笔记标题]]` 两种格式
- 反向链接更新：使用 Map 优化，避免全量遍历

**Editor** (`Editor.tsx`):
- CodeMirror 6 引擎，支持 Markdown 语法高亮
- 三种模式：编辑 / 分屏（50/50）/ 预览（全宽 Markdown 渲染）
- 格式化工具栏：粗体、斜体、删除线、标题、列表、引用、代码块、链接、任务列表
- Vim 模式切换（`@replit/codemirror-vim`）
- 全屏切换
- 光标位置：链接插入时正确放置到 URL 占位符位置

**FileTree** (`FileTree.tsx`):
- 递归 TreeNode 组件渲染嵌套文件夹
- 展开/折叠动画（Framer Motion AnimatePresence）
- 右键菜单：新建笔记、新建文件夹、重命名、删除
- 内联重命名（双击触发）
- 根级"全部笔记"条目显示总数
- 选中态：`bg-blue-50 text-blue-700 dark:bg-blue-900/30 dark:text-blue-300`

**GraphView** (`GraphView.tsx`):
- D3 v7 力导向图，完整重构
- **节点聚类**：基于链接关系自动聚类，10 种颜色区分
- **过滤模式**：全部 / 关联笔记 / 局部邻域
- **缩放控制**：放大 / 缩小 / 重置，显示百分比
- **标签切换**：显示/隐藏节点标签
- **悬停提示**：显示出链数、入链数、标签
- **力模拟参数**：`forceLink(60)`, `forceManyBody(-150)`, `forceCollide(radius+8)`, `forceRadial` 用于孤立节点
- 节点可拖拽，点击跳转笔记
- 正确清理 D3 事件监听器，避免内存泄漏

**CommandPalette** (`CommandPalette.tsx`):
- 模态覆盖层，搜索输入框
- 三种结果类型：动作（新建笔记）、笔记、文件夹
- 键盘导航：上下箭头、Enter 确认、Escape 关闭
- Fuse.js 模糊搜索

### 8.3 任务 (Tasks)

**文件**: `src/pages/Tasks/index.tsx`

**数据源**: `taskDB`

**功能**:
- 添加任务（标题 + 优先级 + 截止日期）
- 完成切换（圆形按钮，勾选 + 删除线）
- 内联编辑（编辑标题/优先级/截止日期）
- 删除（悬停显示操作按钮）
- 逾期检测（红色标记）

**优先级颜色**: 高=rose, 中=amber, 低=emerald

### 8.4 财务 (Finance)

**文件**: `src/pages/Finance/index.tsx`

**数据源**: `financeDB`

**功能**:
- 三张统计卡片：总收入、总支出、净收益
- 收支记录表格：类型切换、日期/模型筛选、编辑/删除
- 模型统计面板：按模型分组展示
- 添加/编辑表单：模态对话框，动态分类列表

**深色模式**: 已适配

### 8.5 工具 (Tools)

**文件**: `src/pages/Tools/index.tsx`

三个独立工具：
1. **计算器** — 四则运算，连续计算支持
2. **取色器** — 实时 HEX/RGB 预览 + 预设颜色网格
3. **JSON 格式化** — 格式化 / 压缩 / 复制 / 清空

无数据持久化。

### 8.6 设置 (Settings)

**文件**: `src/pages/Settings/index.tsx`

左侧边栏 + 右侧内容的标签页布局。

**标签页**:

| 标签 | 功能 |
|---|---|
| profile | 昵称/邮箱/简介 |
| notifications | 通知设置 |
| security | PIN 码设置 |
| appearance | **主题切换**（浅色/深色） |
| data | 数据统计 + 导出 / 导入 / 清除 |

---

## 9. 样式体系

### 设计系统 (v1.3.0 新增)

项目已建立统一的设计系统，包含以下核心要素：

**主色系统** (`tailwind.config.js`):
```javascript
primary: {
  DEFAULT: '#165DFF',  // 低饱和专业蓝
  50: '#E8F1FF',
  100: '#D1E3FF',
  200: '#A3C7FF',
  300: '#75ABFF',
  400: '#478FFF',
  500: '#165DFF',
  600: '#124ACC',  // hover
  700: '#0D3899',  // active
  800: '#092666',
  900: '#041333',
}
```

**语义色**:
- `success: #00B42A` — 成功/完成状态
- `warning: #FF7D00` — 警告/中优先级
- `error: #F53F3F` — 错误/高优先级

**中性色阶** (`neutral 50-900`): 从白到黑的完整灰阶

**圆角系统**: sm(4px) / DEFAULT(6px) / md(8px) / lg(12px) / xl(16px)

**阴影系统**: card / card-hover 用于轻量拟态效果

### CSS 自定义属性（`src/styles/index.css`）

```css
:root {
  /* 背景 */
  --bg-primary: #FFFFFF;
  --bg-secondary: #FAFAFA;
  --bg-tertiary: #F5F5F5;
  --bg-card: #FFFFFF;
  
  /* 文字 */
  --text-primary: #212121;
  --text-secondary: #616161;
  --text-muted: #9E9E9E;
  
  /* 边框 */
  --border-primary: #E0E0E0;
  --border-secondary: #BDBDBD;
  
  /* 主色 */
  --color-primary: #165DFF;
  --color-primary-hover: #124ACC;
  --color-primary-active: #0D3899;
  
  /* 语义色 */
  --color-success: #00B42A;
  --color-warning: #FF7D00;
  --color-error: #F53F3F;
}

.dark {
  --bg-primary: #141414;
  --bg-secondary: #1F1F1F;
  --bg-tertiary: #2A2A2A;
  --bg-card: #1F1F1F;
  
  --text-primary: #FAFAFA;
  --text-secondary: #BDBDBD;
  --text-muted: #757575;
  
  --border-primary: #2A2A2A;
  --border-secondary: #424242;
}
```

### UI 组件库 (`src/components/ui/`)

项目已封装通用 UI 组件，确保样式一致性：

| 组件 | 文件 | 说明 |
|------|------|------|
| Button | `Button.tsx` | 5种变体(primary/secondary/ghost/danger/success)、3种尺寸、loading状态 |
| Card | `Card.tsx` | 3种变体(default/bordered/elevated)、hoverable效果 |
| EmptyState | `EmptyState.tsx` | 统一空状态组件，含图标/标题/描述/操作按钮 |
| Toggle | `Toggle.tsx` | 开关组件，checked时bg-primary |

使用方式：
```tsx
import { Button, Card, EmptyState, Toggle } from '../../components/ui';

<Button variant="primary" onClick={handleClick}>保存</Button>
<EmptyState icon={FileText} title="暂无笔记" action={{ label: '新建', onClick: createNote }} />
<Toggle checked={enabled} onChange={setEnabled} />
```

### 主题机制

- **ThemeContext** (`src/contexts/ThemeContext.tsx`)：提供 `theme`、`setTheme`、`isDark`
- **设置页面**：外观标签页可切换浅色/深色主题
- **持久化**：主题偏好存储于 localStorage (`theme` key)
- **Tailwind 暗色模式**：使用 `class` 策略，所有组件使用 `dark:` 前缀适配

### Markdown 样式

`.markdown-body` 类为 Markdown 预览提供样式，支持深色模式。

### CodeMirror 样式

编辑器样式支持主题切换，使用 CSS 变量。

---

## 10. 已知问题与技术债务

### 已修复问题

1. ~~数据层暴露内部引用~~ → 已封装，通过 API 访问
2. ~~搜索竞态条件~~ → 已添加取消机制
3. ~~内存泄漏~~ → 已正确清理 useEffect 和 D3 事件监听器
4. ~~深色模式不完整~~ → 已全面适配
5. ~~文件夹选中颜色问题~~ → 已修复
6. ~~搜索性能问题~~ → noteDB.search() 现在缓存 Fuse 实例，避免每次创建新实例
7. ~~GraphView D3 内存泄漏~~ → 正确清理 simulation 和事件监听器
8. ~~GraphView adjustColorForDark 无效~~ → 已实现深色模式颜色调整
9. ~~Editor 缺少自动保存~~ → 添加 800ms 防抖自动保存和状态指示器
10. ~~Editor 缺少快捷键~~ → 添加 Ctrl+B/I/U/S/Z/Y 等快捷键支持
11. ~~Editor 缺少撤销/重做~~ → 启用 CodeMirror 内置历史记录
12. ~~Editor 分屏同步滚动~~ → 已实现编辑器与预览区域联动滚动
13. ~~FileTree 删除无确认~~ → 添加删除确认对话框
14. ~~useNotesSearch 状态管理混乱~~ → 修复 isSearching 状态和 useMemo 包裹 async 函数问题
15. ~~Notes handleUpdateNote 性能问题~~ → 优化状态更新逻辑，避免不必要的 Map 重建
16. ~~标题输入无防抖~~ → handleUpdateTitle 添加 500ms 防抖
17. ~~Folder 嵌套有限~~ → `folderDB.getTree()` 已支持无限层级嵌套，FileTree 组件使用递归 TreeNode 渲染
18. ~~隐式数据关联~~ → 笔记重命名时自动更新其他笔记中的 `[[旧标题]]` 链接引用
19. ~~组件体积过大~~ → FileTree 拆分为 NoteItem、TreeNode、NoteContextMenu、FolderContextMenu 子组件；GraphView 拆分为 types、useGraphSimulation、useGraphData、GraphControls 子模块

### 剩余技术债务

无

---

## 11. 常见开发任务

### 新增页面

1. 在 `src/pages/` 下创建目录和组件
2. 在 `src/App.tsx` 添加路由
3. 在 `Sidebar.tsx` 的 `menuItems` 数组中添加菜单项
4. 如需数据持久化，使用 `src/db/index.ts` 中对应集合的 API

### 新增数据集合

1. 在 `src/db/index.ts` 中定义 TypeScript 接口
2. 添加 localStorage key 常量
3. 创建 CRUD 操作对象
4. 在 `initDB()`、`dataManager` 中注册新集合
5. 在 `CLAUDE.md` 中更新数据层文档

### 修改主题

- 使用 `useTheme()` hook 获取当前主题
- 组件样式使用 `dark:` 前缀
- CSS 变量使用 `var(--bg-primary)` 等

### 调试提示

- 数据存储在 localStorage，可在浏览器 DevTools → Application → Local Storage 中查看
- 启动 `npm run dev` 后默认访问 `http://localhost:3266/`
- 类型检查：`npm run typecheck`

### 构建部署

1. `npm run build` → 输出到 `dist/bundle.js`
2. 将 `dist/` 目录部署到静态服务器
3. 注意：应用使用 HashRouter，部署时无需配置 URL 重写规则

---

## 12. 近期更新记录

### v1.5.0 (2026-04-29)

**技术债务清零**

- **Folder 嵌套修复**:
  - `folderDB.getTree()` 已支持无限层级嵌套
  - FileTree 组件使用递归 TreeNode 渲染多层文件夹结构

- **链接引用更新**:
  - 笔记重命名时自动更新其他笔记中的 `[[旧标题]]` 链接引用
  - handleUpdateTitle 添加链接内容替换逻辑

- **组件拆分优化**:
  - FileTree 拆分为独立子组件：
    - `FileTree/NoteItem.tsx` - 笔记条目组件
    - `FileTree/TreeNode.tsx` - 文件夹节点组件（递归渲染）
    - `FileTree/NoteContextMenu.tsx` - 笔记右键菜单
    - `FileTree/FolderContextMenu.tsx` - 文件夹右键菜单
  - GraphView 拆分为独立模块：
    - `GraphView/types.ts` - 类型定义（GraphNode、GraphLink、COLORS）
    - `GraphView/useGraphSimulation.ts` - D3 力导向图模拟 hook
    - `GraphView/useGraphData.ts` - 图谱数据构建 hook
    - `GraphView/GraphControls.tsx` - 控制面板和统计组件

- **useDB hook 修复**:
  - 修复 dependencies 数组作为 useEffect 依赖导致的无限循环问题
  - 使用 depsRef 包装 dependencies，避免不必要的重新执行

### v1.4.0 (2026-04-29)

**技术债务优化**

- **数据库层优化**:
  - noteDB.search() 添加 Fuse 实例缓存，避免每次搜索创建新实例
  - 修复 useDB hook depsKey 字符串化问题

- **GraphView 优化**:
  - 修复 adjustColorForDark 函数无效问题，正确实现深色模式颜色调整
  - 改进 D3 simulation 和事件监听器清理，避免内存泄漏

- **Editor 增强**:
  - 添加自动保存功能（800ms 防抖）+ 保存状态指示器
  - 添加快捷键支持：Ctrl+B(粗体)、Ctrl+I(斜体)、Ctrl+U(删除线)、Ctrl+S(手动保存)、Ctrl+Z/Y(撤销/重做)
  - 启用 CodeMirror 内置历史记录
  - 分屏模式添加同步滚动

- **FileTree 增强**:
  - 添加删除确认对话框，防止误操作

- **Notes 模块优化**:
  - 修复 useNotesSearch hook 中 isSearching 状态管理
  - 优化 handleUpdateNote 状态更新逻辑，避免不必要的 Map 重建
  - 标题输入添加 500ms 防抖，减少频繁写入

### v1.3.0 (2026-04-29)

**UI 设计系统重构**

- **设计令牌体系**:
  - 主色改为低饱和专业蓝 `#165DFF`（50-900色阶）
  - 新增语义色：success(#00B42A)、warning(#FF7D00)、error(#F53F3F)
  - 建立中性色阶 neutral 50-900
  - 统一圆角系统：sm(4px)、default(6px)、md(8px)、lg(12px)
  - 新增阴影系统：card、card-hover（轻量拟态效果）

- **通用 UI 组件**:
  - 创建 `src/components/ui/` 目录
  - Button 组件：5种变体、3种尺寸、loading状态、hover动画
  - Card 组件：3种变体、hoverable效果、padding配置
  - EmptyState 组件：统一空状态样式（图标容器w-16、标准文案格式）
  - Toggle 组件：统一开关样式（checked=bg-primary，unchecked=bg-neutral-300）

- **页面样式迁移**:
  - Dashboard：按钮、链接、空状态统一样式
  - Tasks：优先级选择改为按钮组（带图标）、筛选按钮、空状态
  - Settings：Toggle组件化、Tab选中态统一
  - Finance：图表颜色用设计系统色板、筛选按钮统一
  - Notes：FileTree选中态、TagCloud、GraphView节点颜色、Editor主题动态切换
  - Tools：计算器按钮样式
  - Loading：加载动画颜色改为主色

- **交互优化**:
  - 移除 Framer Motion whileHover 的 backgroundColor 动画，改用纯CSS hover
  - 过渡时间统一为 150ms（快速响应）
  - focus:ring-2 focus:ring-primary/20 统一 focus 状态

- **图表颜色统一**:
  - Finance CHART_COLORS: ['#165DFF', '#00B42A', '#FF7D00', '#F53F3F', '#757575', ...]
  - GraphView COLORS.cluster: 使用主色系色阶

### v1.2.0 (2026-04-29)

**主题系统**
- 新增 `ThemeContext` 提供主题切换能力
- 设置页面新增"外观"标签页，支持浅色/深色切换
- 所有页面和组件完成深色模式适配

**笔记模块优化**
- GraphView 完整重构：
  - 节点聚类与颜色编码
  - 三种过滤模式（全部/关联/局部）
  - 缩放控制与百分比显示
  - 标签切换
  - 悬停提示（出链/入链/标签）
  - 正确清理 D3 事件监听器
- 链接解析优化：支持 ID 和标题两种格式，使用 Set 去重
- 反向链接更新：使用 Map 优化性能
- 搜索防抖：添加取消机制避免竞态条件
- Editor 光标位置：链接插入时正确放置到 URL 占位符

**数据层改进**
- 移除内部引用暴露，数据封装通过 API 访问
- 修复 TypeScript 类型错误

**UI 修复**
- Dashboard 任务卡片和净收益区域深色模式适配
- FileTree 文件夹选中时名称和图标颜色修复
- 添加 ErrorBoundary、Loading、PinLock、VirtualList 组件

### v1.1.0 (2026-04-28)

- 初始版本发布
- 6 个功能模块：仪表盘、笔记、任务、财务、工具、设置
- localStorage 数据持久化
- Webpack 5 构建配置
- auto-sync 开发服务器同步

---

## 13. 下一步计划：接入 AI 大模型

### 13.1 目标概述

将 AI 大模型能力集成到个人工作站，实现智能辅助功能，提升用户效率。

### 13.2 功能规划

| 功能模块 | AI 能力 | 实现方案 |
|----------|---------|----------|
| **笔记** | 智能写作助手 | 润色、扩写、摘要生成、标题建议 |
| **笔记** | Markdown 增强 | 自动格式化、代码解释、公式转换 |
| **任务** | 智能任务分析 | 自动拆解复杂任务、预估时间、优先级建议 |
| **财务** | 费用洞察 | API 使用趋势分析、异常检测、预算建议 |
| **工具** | AI 对话 | 新增 AI 聊天工具页面，支持多轮对话 |
| **全局** | 智能搜索 | 自然语言搜索笔记/任务/财务记录 |

### 13.3 技术方案

**API 选择**:
- 推荐 Claude API (Anthropic) 或 OpenAI API
- 国内：DeepSeek、智谱 GLM、阿里百炼等

**配置存储**:
```typescript
interface AIConfig {
  provider: 'anthropic' | 'openai' | 'deepseek' | 'glm';
  apiKey: string;  // 存储于 localStorage
  model: string;   // claude-4-sonnet / gpt-4 / deepseek-chat
  baseUrl?: string; // 自定义 API 地址
}
```

**新增文件**:
```
src/
├── services/
│   └── ai.ts              # AI API 调用封装
│   └── prompts.ts         # Prompt 模板库
├── contexts/
│   └── AIContext.tsx      # AI 配置上下文
├── pages/
│   └── AIChat/
│   │   └── index.tsx      # AI 聊天页面
│   │   └── components/
│   │       ├── ChatInput.tsx
│   │       └── ChatMessage.tsx
│   └── Settings/
│       └── AIConfigTab.tsx # AI 配置标签页
```

**安全考虑**:
- API Key 仅存储于本地 localStorage，不上传服务器
- 提供清除 API Key 功能
- API 调用需用户手动触发，不自动调用

### 13.4 UI 集成点

1. **设置页新增标签页**: "AI 配置"
   - API 提供商选择
   - API Key 输入（支持显示/隐藏）
   - 模型选择
   - 测试连接按钮

2. **笔记编辑器**: 工具栏新增 AI 按钮
   - "AI 润色" / "生成摘要" / "续写" / "改写标题"
   - 选中文本后右键菜单 AI 操作

3. **任务页**: 添加任务时 AI 辅助
   - 复杂任务自动拆解
   - 截止日期预估

4. **新增 AI 聊天页面**: 侧边栏新增入口
   - 多轮对话界面
   - 历史记录存储
   - 快捷指令（如 "/summarize"）

### 13.5 实施步骤

| 步骤 | 内容 | 预计时间 |
|------|------|----------|
| 1 | 创建 AI 服务层 (`src/services/ai.ts`) | 1h |
| 2 | AI 配置上下文 + 设置页标签 | 2h |
| 3 | AI 聊天页面 | 3h |
| 4 | 笔记 AI 工具栏集成 | 2h |
| 5 | 任务 AI 辅助功能 | 2h |
| 6 | 测试与优化 | 2h |

### 13.6 注意事项

- **成本控制**: 提供 token 计数显示，用户可选择模型
- **响应时间**: 长文本处理显示 loading 状态
- **错误处理**: API 错误友好提示，支持重试
- **数据隐私**: 明确告知用户数据会发送到 AI API
- **离线可用**: AI 功能可选，不影响核心功能使用

---

## 14. 笔记模块深度问题分析

### 14.1 功能缺陷

#### 编辑器功能不完整

| 问题 | 文件位置 | 具体表现 |
|------|----------|----------|
| 缺少自动保存 | Editor.tsx:19-25 | 每次 onChange 直接触发保存，无防抖，无保存状态指示器 |
| 缺少快捷键 | Editor.tsx:60-63 | 工具栏无快捷键提示，未绑定 Ctrl+B/I 等常用快捷键 |
| 缺少撤销/重做 | Editor.tsx | 无 UI 操作撤销历史记录 |
| 缺少高级功能 | Editor.tsx:69-140 | 无表格插入、图片上传、数学公式支持 |
| 分屏同步滚动缺失 | Editor.tsx:216-239 | split 模式下编辑器和预览区域无联动滚动 |

#### 文件树操作问题

| 问题 | 文件位置 | 具体表现 |
|------|----------|----------|
| 缺少批量操作 | FileTree.tsx | 无法批量删除/移动笔记，无法批量添加标签 |
| 拖拽排序缺失 | FileTree.tsx:229-233 | 只支持拖拽到文件夹，不支持同文件夹内自定义排序 |
| 重命名无确认 | FileTree.tsx:291-296 | onBlur 直接保存，无撤销功能，易误操作 |
| 文件夹不可移动 | FileTree.tsx | 只能拖拽笔记，文件夹本身无法移动到其他文件夹 |

#### 搜索/筛选效率问题

| 问题 | 文件位置 | 具体表现 |
|------|----------|----------|
| 搜索性能瓶颈 | useNotesSearch.ts:21-47, db/index.ts:631-645 | 每次搜索创建新 Fuse 实例，大量笔记时性能差 |
| 缺少高级搜索 | Notes/index.tsx:220-264 | 只支持关键词，无时间范围/标签/文件夹组合筛选 |
| 搜索状态混乱 | useNotesSearch.ts:8-9,49-51 | isSearching 声明但从未正确设置，UI 无法显示加载状态 |
| 结果排序不直观 | db/index.ts:631-645 | Fuse.js 按相关性排序，用户可能期望按时间排序 |

#### 双向链接可靠性问题

| 问题 | 文件位置 | 具体表现 |
|------|----------|----------|
| 链接解析漏洞 | Notes/index.tsx:96-121 | `linkText.length > 10 && !linkText.includes(' ')` 判断可能误判短标题笔记 |
| 链接更新竞态 | Notes/index.tsx:127-130 | 同时调用 noteDB.update 和 updateLinks 可能竞态 |
| 删除链接残留 | db/index.ts:621-625 | 删除笔记只清理 backlinks，内容中 `[[deleted_id]]` 链接仍残留 |
| 迁移逻辑简单 | db/index.ts:486-539 | `l.includes(' ') || l.length < 5` 判断过于简单，可能误判合法 ID |

#### 标签系统不完善

| 问题 | 文件位置 | 具体表现 |
|------|----------|----------|
| 缺少标签管理 | TagCloud.tsx | 只显示和筛选，无法创建/删除/重命名/合并标签 |
| 编辑器无标签按钮 | Editor.tsx | 工具栏无标签添加按钮，用户需手动编辑内容 |
| 筛选只支持 OR | Notes/index.tsx:199-201 | `selectedTags.some(...)` 是 OR 逻辑，无 AND 选项 |
| 标签统计不缓存 | TagCloud.tsx:16-24 | 每次渲染重新计算标签统计，无缓存机制 |

#### 图谱视图实用性问题

| 问题 | 文件位置 | 具体表现 |
|------|----------|----------|
| 交互功能有限 | GraphView.tsx | 无法通过图谱创建新连接，无法保存拖拽布局 |
| 缺少导出功能 | GraphView.tsx | 无法导出图谱为图片或 JSON |
| 性能问题 | GraphView.tsx:202-387 | filteredData 变化时重建整个 SVG 和 simulation，大量节点卡顿 |
| adjustColorForDark 无效 | GraphView.tsx:553-555 | 函数直接返回原颜色，暗色模式某些颜色不清晰 |

### 14.2 用户体验问题

| 类别 | 问题 | 文件位置 |
|------|------|----------|
| 交互流畅性 | 标题输入无防抖，频繁写入数据库 | Notes/index.tsx:268-276 |
| 交互流畅性 | 右面板切换无过渡动画 | Notes/index.tsx:380-401 |
| 交互流畅性 | 文件树展开动画与 guide line 不匹配 | FileTree.tsx:439-483 |
| 交互流畅性 | 拖拽反馈只有 ring-2，视觉反馈弱 | FileTree.tsx:298-316 |
| 反馈不及时 | 缺少 "已保存/保存中" 状态指示 | Notes/index.tsx |
| 反馈不及时 | 删除操作无确认对话框 | FileTree.tsx:103-117 |
| 反馈不及时 | 快捷键提示 `⌘K` 对 Windows 用户不友好 | Notes/index.tsx:261 |
| 空状态不友好 | 缺少详细引导和示例 | Notes/index.tsx:350-363 |
| 空状态不友好 | TagCloud 只显示 "暂无标签" | TagCloud.tsx:35-42 |
| 空状态不友好 | GraphView 缺少功能说明 | GraphView.tsx:513-519 |
| 错误处理缺失 | noteDB.add 无 catch 处理 | Notes/index.tsx:77-91 |
| 错误处理缺失 | handleUpdateNote 多操作无 try-catch | Notes/index.tsx:93-165 |
| 错误处理缺失 | localStorage 写入失败无 UI 反馈 | db/index.ts:147-170 |

### 14.3 技术债务

| 类别 | 问题 | 文件位置 |
|------|------|----------|
| 代码质量 | useNotesSearch hook 实现冗余，useMemo 包裹 async 函数返回新引用 | useNotesSearch.ts:21-47 |
| 代码质量 | 过度使用 useCallback/useMemo（53处），很多无意义缓存 | 全模块 |
| 代码质量 | FileTree 组件 648 行过于庞大，含多个内部组件定义 | FileTree.tsx |
| 代码质量 | Fuse.js 搜索逻辑重复实现于 useNotesSearch、db/index.ts、CommandPalette.tsx | 多文件 |
| 代码质量 | adjustColorForDark 函数无效，直接返回原颜色 | GraphView.tsx:553-555 |
| 性能瓶颈 | GraphView useEffect 重建开销大，依赖触发整个 SVG/simulation 重建 | GraphView.tsx:202-387 |
| 性能瓶颈 | 搜索 debounce 期间创建多个 Promise | useNotesSearch.ts:21-47 |
| 性能瓶颈 | handleUpdateNote 用 Map 重建整个数组触发不必要渲染 | Notes/index.tsx:134-162 |
| 性能瓶颈 | useDB depsKey 字符串化可能导致不必要效果触发 | db/index.ts:876 |
| 内存泄漏风险 | D3 simulation 只 stop()，selection 可能持有引用 | GraphView.tsx:377-386 |
| 内存泄漏风险 | 全局 listeners Set 未清理时监听器残留 | db/index.ts:125 |
| 类型安全 | GraphLink source/target 混合 string | GraphNode 类型 | GraphView.tsx:28-31 |
| 类型安全 | 频繁类型断言 `typeof link.source === 'string' ? ... : ...` | GraphView.tsx 多处 |
| 类型安全 | TransactionOperation 使用 any 类型 | db/index.ts:325 |

### 14.4 架构问题

| 类别 | 问题 | 文件位置 |
|------|------|----------|
| 组件划分 | FileTree 648 行应拆分为 NoteItem、TreeNode、ContextMenu 等独立文件 | FileTree.tsx |
| 组件划分 | Editor 职责不清，应拆分为 Toolbar、CodeEditor、PreviewPanel | Editor.tsx |
| 组件划分 | GraphView 572 行，D3 逻辑应提取为 useGraphSimulation hook | GraphView.tsx |
| 状态管理 | 状态分散在多层级：notes/folders/currentNote/selectedTags 在顶层，filteredNotes 渲染时计算 | Notes/index.tsx |
| 状态管理 | 数据同步逻辑复杂，依赖 data + loading 组合条件难以追踪 | Notes/index.tsx:49-55 |
| 状态管理 | 回调函数层层传递：onSelectNote/onSelectFolder/onCreateNote 链路长 | Notes/index.tsx → FileTree |
| 状态管理 | Context 使用不一致：FileTree 用 Context，Backlinks/TagCloud 用 props | 多组件 |
| 数据流 | 双向链接更新流程过长（6步），任何一步失败导致数据不一致 | Notes/index.tsx:93-165 |
| 数据流 | useDB 返回 data 复制到 state，双重存储易不一致 | Notes/index.tsx |
| 数据流 | reloadData 函数重复逻辑，应统一数据获取 | Notes/index.tsx:57-64 |
| 数据流 | 数据库 subscribe 机制未使用，依赖手动 reloadData | db/index.ts vs Notes/index.tsx |

### 14.5 其他问题

| 类别 | 问题 |
|------|------|
| 可访问性 | NoteContextMenu ArrowUp/ArrowDown 都设置为 selectedIndex=0 |
| 可访问性 | 缺少 aria-label 动态更新（如包含具体笔记名称） |
| 可访问性 | 删除笔记后焦点无明确去向 |
| 国际化 | 所有文本硬编码中文，无 i18n 库 |
| 移动端 | 拖拽调整宽度只支持 mousemove/mouseup，无 touch 事件 |

---

## 15. 下一步计划：AI 辅助个人知识库

### 15.1 目标定位

将笔记模块从基础 Markdown 编辑器升级为 **AI 辅助的个人知识库**，实现：
- 智能内容生成与优化
- 知识图谱自动分析与可视化
- 智能检索与关联推荐
- 笔记质量评估与改进建议

### 15.2 功能规划

#### AI 编辑助手

| 功能 | 描述 | 实现方案 |
|------|------|----------|
| 智能续写 | 根据上下文自动续写内容 | AI API 调用，提供前文作为 context |
| 内容润色 | 优化表达、修正语法、提升可读性 | 选中文本 → AI 润色 → 替换 |
| 自动摘要 | 生成笔记摘要用于预览和搜索 | 分析全文，输出 100-200 字摘要 |
| 标题建议 | 根据内容生成多个标题选项 | AI 分析内容，输出 3-5 个候选标题 |
| 多语言翻译 | 支持中英文互译 | 选中文本 → AI 翻译 → 插入 |
| 格式转换 | Markdown ↔ 富文本 ↔ 纯文本 | AI 解析并转换格式 |

#### 知识图谱增强

| 功能 | 描述 | 实现方案 |
|------|------|----------|
| 智能链接推荐 | 编辑时推荐相关笔记链接 | AI 分析内容相似度，推荐 top-5 笔记 |
| 自动分类建议 | 新建笔记时建议归属文件夹 | AI 分析内容主题，匹配已有文件夹 |
| 知识脉络提取 | 从笔记群提取主题脉络 | AI 分析笔记群，生成知识树 |
| 关联强度计算 | 用 AI 评估笔记间关联程度 | 内容相似度 + 链接频率综合评分 |
| 图谱交互增强 | 支持在图谱上创建连接、合并节点 | UI 改造 + AI 辅助决策 |

#### 智能检索

| 功能 | 描述 | 实现方案 |
|------|------|----------|
| 自然语言搜索 | "上周关于 React 的笔记" → 返回结果 | AI 解析意图，转换为搜索条件 |
| 语义搜索 | 搜索概念而非关键词 | 向量嵌入 + 相似度匹配 |
| 智能问答 | "笔记里怎么配置 webpack？" → 返回答案 | AI 检索相关笔记，综合回答 |
| 相关推荐 | 查看笔记时推荐相关内容 | 内容分析 + 协同过滤 |

#### 笔记质量评估

| 功能 | 描述 | 实现方案 |
|------|------|----------|
| 完整度评分 | 评估笔记内容完整性 | AI 检查结构、引用、结论 |
| 可读性分析 | 分析语言流畅度、逻辑清晰度 | AI 文本分析 |
| 改进建议 | 给出具体的改进建议 | AI 分析后输出建议列表 |
| 知识覆盖图 | 可视化用户知识领域覆盖情况 | 主题提取 + 领域映射 |

### 15.3 技术改造计划

#### 阶段一：编辑器 AI 增强（预计 8h）

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 1.1 | Editor 工具栏添加 AI 按钮（润色/续写/摘要/翻译） | Editor.tsx |
| 1.2 | 实现 AI 操作 UI（选中文本 → 右键菜单 → AI 操作） | Editor.tsx |
| 1.3 | 创建 EditorToolbar 组件分离工具栏逻辑 | components/EditorToolbar.tsx |
| 1.4 | 实现自动保存与保存状态指示器 | Editor.tsx, Notes/index.tsx |
| 1.5 | 添加快捷键支持（Ctrl+B/I 等）并显示提示 | Editor.tsx |
| 1.6 | 实现分屏模式同步滚动 | Editor.tsx |

#### 阶段二：知识图谱 AI 增强（预计 6h）

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 2.1 | GraphView 添加智能链接推荐功能 | GraphView.tsx |
| 2.2 | 实现关联强度可视化（线条粗细/颜色） | GraphView.tsx |
| 2.3 | 添加图谱交互：创建连接、合并节点 | GraphView.tsx, FileTree.tsx |
| 2.4 | 实现图谱导出（PNG/JSON） | GraphView.tsx |
| 2.5 | 优化性能：增量更新而非重建 | GraphView.tsx |

#### 阶段三：智能检索系统（预计 6h）

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 3.1 | CommandPalette 添加自然语言搜索 | CommandPalette.tsx |
| 3.2 | 实现语义搜索（向量嵌入） | services/embedding.ts |
| 3.3 | 添加智能问答入口 | 新增 AIChat 集成到 Notes |
| 3.4 | 搜索结果添加相关推荐 | CommandPalette.tsx |
| 3.5 | 优化 Fuse.js 性能（复用实例、缓存索引） | db/index.ts, useNotesSearch.ts |

#### 阶段四：架构重构（预计 8h）

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 4.1 | FileTree 拆分为独立子组件 | FileTree.tsx → FileTree/ |
| 4.2 | 提取共享 UI 组件（Button/Input/Modal 已完成） | components/ui/ |
| 4.3 | 统一状态管理：创建 NotesContext | contexts/NotesContext.tsx |
| 4.4 | 双向链接更新改为事务操作 | db/index.ts |
| 4.5 | 使用数据库 subscribe 机制替代手动 reloadData | Notes/index.tsx |
| 4.6 | 添加错误处理与 UI 反馈 | Notes/index.tsx, db/index.ts |

#### 阶段五：质量与体验（预计 4h）

| 任务 | 内容 | 涉及文件 |
|------|------|----------|
| 5.1 | 笔记质量评分面板 | 新增 components/QualityPanel.tsx |
| 5.2 | 空状态优化（详细引导） | Notes/index.tsx, TagCloud.tsx, GraphView.tsx |
| 5.3 | 删除操作添加确认对话框 | FileTree.tsx |
| 5.4 | 移动端 touch 事件支持 | useNotesLayout.ts |
| 5.5 | 可访问性修复（键盘导航、aria-label） | FileTree.tsx, GraphView.tsx |

### 15.4 新增文件结构

```
src/
├── services/
│   ├── ai.ts                 # AI API 调用封装
│   ├── embedding.ts          # 向量嵌入服务
│   └── prompts/
│       ├── editor.ts         # 编辑器相关 Prompt
│       ├── search.ts         # 搜索相关 Prompt
│       └── graph.ts          # 图谱相关 Prompt
│
├── contexts/
│   ├── AIContext.tsx         # AI 配置上下文（复用 Settings）
│   └── NotesContext.tsx      # 笔记模块状态上下文
│
├── pages/Notes/
│   ├── index.tsx             # 主页面（简化）
│   ├── components/
│   │   ├── Editor/
│   │   │   ├── index.tsx     # 编辑器主组件
│   │   │   ├── Toolbar.tsx   # 工具栏（含 AI 按钮）
│   │   │   ├── Preview.tsx   # 预览面板
│   │   │   └── AIPanel.tsx   # AI 操作面板
│   │   ├── FileTree/
│   │   │   ├── index.tsx     # 文件树主组件
│   │   │   ├── NoteItem.tsx  # 笔记条目
│   │   │   ├── TreeNode.tsx  # 文件夹节点
│   │   │   ├── ContextMenu.tsx # 右键菜单
│   │   │   └── ConfirmDialog.tsx # 删除确认
│   │   ├── GraphView/
│   │   │   ├── index.tsx     # 图谱主组件
│   │   │   ├── useSimulation.ts # D3 simulation hook
│   │   │   ├── NodeRenderer.tsx # 节点渲染
│   │   │   └ LinkRenderer.tsx  # 连线渲染
│   │   │   └ Controls.tsx    # 控制面板
│   │   │   └ ExportDialog.tsx # 导出对话框
│   │   ├── QualityPanel.tsx  # 笔记质量评分
│   │   ├── SmartSearch.tsx   # 智能搜索面板
│   │   └ Recommendations.tsx # 相关推荐
│   │   └── ...
│   └── hooks/
│       ├── useNotesState.ts  # 统一状态管理
│       ├── useAutoSave.ts    # 自动保存
│       ├── useAIOperations.ts # AI 操作封装
│       └── ...
```

### 15.5 AI Prompt 模板示例

```typescript
// src/services/prompts/editor.ts
export const PROMPTS = {
  polish: `请润色以下文本，保持原意但提升可读性和表达流畅度。输出润色后的文本：
---
{content}
---`,

  summarize: `请为以下笔记生成一个简洁的摘要（100-200字），用于笔记预览和搜索：
---
{content}
---`,

  suggestTitle: `请根据以下笔记内容，生成3-5个合适的标题建议，按推荐程度排序：
---
{content}
---`,

  continue: `请根据以下上下文续写内容，保持风格一致：
---
{previousContent}
---
请续写约 {length} 字。`,

  relatedNotes: `请分析以下笔记内容，推荐5个最相关的已存在笔记主题：
---
{content}
---
已有笔记标题列表：{existingTitles}`,

  qualityAssess: `请评估以下笔记的质量，从完整性、可读性、结构清晰度三个维度给出评分（1-10）和改进建议：
---
{content}
---`,

  naturalLanguageSearch: `请将以下自然语言搜索请求转换为搜索条件：
---
用户输入："{query}"
---
输出格式：{
  keywords: string[],
  dateRange?: { start?: string, end?: string },
  tags?: string[],
  folder?: string
}`,
};
```

### 15.6 数据模型扩展

```typescript
// 扩展 Note 接口
interface Note {
  // ...原有字段
  summary?: string;           // AI 生成的摘要
  qualityScore?: number;      // 质量评分
  embedding?: number[];       // 向量嵌入（用于语义搜索）
  aiSuggestions?: {           // AI 建议（可忽略）
    relatedNotes?: string[];
    suggestedTags?: string[];
    suggestedFolder?: string;
  };
}

// 新增笔记质量评估结果
interface QualityAssessment {
  noteId: string;
  completeness: number;       // 完整度 1-10
  readability: number;        // 可读性 1-10
  structure: number;          // 结构清晰度 1-10
  suggestions: string[];      // 改进建议
  assessedAt: string;
}
```

### 15.7 实施时间表

| 阶段 | 内容 | 预计时间 | 优先级 |
|------|------|----------|--------|
| 阶段一 | 编辑器 AI 增强 | 8h | P0 |
| 阶段二 | 知识图谱 AI 增强 | 6h | P1 |
| 阶段三 | 智能检索系统 | 6h | P1 |
| 阶段四 | 架构重构 | 8h | P2 |
| 阶段五 | 质量与体验 | 4h | P2 |
| **总计** | | **32h** | |

### 15.8 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| AI API 成本过高 | 用户放弃使用 | 提供多种模型选择，显示 token 计数，设置使用限额 |
| 向量嵌入存储开销大 | localStorage 撞墙 | 嵌入可选，只对重要笔记生成；或迁移到 IndexedDB |
| AI 响应慢影响体验 | 用户等待烦躁 | 显示进度条，支持取消，异步处理后台任务 |
| 架构重构引入 Bug | 功能损坏 | 分阶段重构，每阶段测试通过后再继续 |
| Prompt 效果不稳定 | AI 输出质量差 | Prompt 版本管理，效果评估，用户反馈收集 |
